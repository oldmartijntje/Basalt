<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>
        <%= title || 'My Site' %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
</head>

<body>
    <header>
        <nav>
            <a href="/">Home</a> |
            <a href="/blogs">Blogs</a> |
            <% if (user) { %>
                Hello <%= user.username || user.name %> | <a href="/logout">Logout</a>
                    <% } else { %>
                        <a href="/login">Login</a>
                        <% } %>
        </nav>
    </header>